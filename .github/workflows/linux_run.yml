name : Build and Test on Ubuntu 24.04 with x86/ARM arch and GCC Clang

on:
  workflow_call:
    inputs:
      compiler:
        required: true
        type: string
      runner:
        required: true
        type: string
      arch:
        required: true
        type: string
      platform:
        required: true
        type: string
    secrets:
      SERVER:
        required: true
      USERNAME:
        required: true
      PW:
        required: true
      GHCR_PAT:
        required: true

env:
  # One place for the image reference; it was hard-coded in both the pull and
  # the run step. Mirrors GHCR_IMAGE_WIN in windows_run.yml.
  GHCR_IMAGE_LINUX: ghcr.io/kataglyphis/kataglyphis_beschleuniger:latest

jobs:
  build:
    runs-on: ${{ inputs.runner }} # Dynamically use input
    defaults:
      run:
        shell: bash

    steps:

      # ContainerHub's shared prologue: reclaim runner disk, check out, log in to
      # ghcr and pull with retries. Replaces four hand-rolled steps including a
      # copy of the retry loop, so the retry count and per-attempt timeout live
      # in one place for every repo instead of drifting per workflow.
      - name: Prepare CI host
        uses: Kataglyphis/Kataglyphis-ContainerHub/.github/actions/prepare-linux-ci-host@main
        with:
          image: ${{ env.GHCR_IMAGE_LINUX }}
          registry-username: ${{ github.actor }}
          registry-password: ${{ secrets.GHCR_PAT }}
          fetch-depth: '0'
          submodules: recursive

      #################################################################################################################
      ######################################## Prepare deps ###########################################################
      #################################################################################################################
      # run-in-linux-container instead of a hand-rolled `docker run`. extra-args
      # is substituted verbatim into the action's docker command line, so the
      # -e values are quoted here rather than passed through a step-level env:
      # an action's own run block does not inherit the CALLER STEP's env, only
      # the job's.
      - name: Run build/test/docs/package inside container
        uses: Kataglyphis/Kataglyphis-ContainerHub/.github/actions/run-in-linux-container@main
        with:
          image: ${{ env.GHCR_IMAGE_LINUX }}
          extra-args: >-
            --privileged --platform ${{ inputs.platform }}
            -e CXXFLAGS="${{ inputs.compiler == 'clang' && '--gcc-toolchain=/opt/gcc-15.2.0' || '' }}"
            -e LDFLAGS="${{ inputs.compiler == 'clang' && '-L/opt/gcc-15.2.0/lib64 -Wl,-rpath,/opt/gcc-15.2.0/lib64 --gcc-toolchain=/opt/gcc-15.2.0' || '' }}"
          script: >-
            bash -lc 'set -e; bash scripts/linux/ci-run-all.sh
            --compiler "${{ inputs.compiler }}"
            --runner "${{ inputs.runner }}"
            --arch "${{ inputs.arch }}"
            --build-type "Debug"
            --build-dir "build"
            --build-release-dir "build-release"
            --gcc-debug-preset "linux-debug-GNU"
            --clang-debug-preset "linux-debug-clang"
            --gcc-profile-preset "linux-profile-GNU"
            --clang-profile-preset "linux-profile-clang"
            --clang-release-preset "linux-release-clang"
            --coverage-json "coverage.json"
            --docs-out "build/build/html"
            --release-callgrind "0"
            --release-appimage "1"
            --release-flatpak "1"
            --release-appimage-out-dir ""
            --release-flatpak-out-dir ""'

      - name: Upload Linux Packages (Clang only)
        if: ${{ inputs.compiler == 'clang' }}
        uses: actions/upload-artifact@v6.0.0
        with:
          name: linux-packages
          path: |
            build-release/*.tar.gz
            build-release/*.tgz
            build-release/*.deb
            build-release/*.AppImage
            build-release/*.flatpak

      - name: Fix permissions for doc directory
        if: ${{ inputs.runner == 'ubuntu-24.04' && inputs.compiler == 'clang' }}
        run: |
          if [[ -d "./docs/build/html" ]]; then
            chmod -R 755 ./docs/build/html/
            ls -la ./docs/build/html/
          else
            echo "No docs directory found at ./docs/build/html (skipping chmod)."
          fi

      - name: Sync files to domain
        if: inputs.runner == 'ubuntu-24.04' && inputs.compiler == 'clang'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.SERVER }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PW }}
          local-dir: "./docs/build/html/"
